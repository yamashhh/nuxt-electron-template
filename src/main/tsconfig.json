{
  // HACK:
  // 本当は@tsconfig/node16を使いたいが
  // 現構成のTypeScriptがv4.2.4で
  // "es2021"がオプションとして認識されず
  // compilerOptionsの"lib""target"上書きもうまくいかなかったため
  // 仕方なくnode14のconfigを使用している
  "extends": "@tsconfig/node14/tsconfig.json",
  "compilerOptions": {
    "outDir": "../../dist"
  }
}
